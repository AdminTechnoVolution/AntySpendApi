# Dependencies
/node_modules

# Build
/dist
/build
*.tsbuildinfo

# Environment (never commit secrets)
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Tests
/coverage
/.nyc_output

# OS
.DS_Store
Thumbs.db

# IDE
.idea
.vscode
!.vscode/settings.json.example
*.swp
*.swo
node_modules
node_modules
# This repo is pnpm-managed (pnpm-lock.yaml is the real lockfile, CI uses pnpm too) — an
# npm-generated package-lock.json here is stale by construction and caused a real `npm ci`
# failure once already (mongoose resolved to a newer, type-breaking version than pnpm's lock).
package-lock.json
